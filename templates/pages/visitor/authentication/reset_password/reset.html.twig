{% extends 'themes/base_visitor.html.twig' %}

{% block title %}Nouveau mot de passe{% endblock %}

{% block description %}Nouveau mot de passe{% endblock %}

{% block keywords %}nouveau, mot-de-passe{% endblock %}

{% block main %}
    <h1 class="text-center my-3 display-5">Nouveau mot de passe</h1>

    <div class="container p-4">
        <div class="row">
            <div class="col-md-8 col-lg-4 p-4 mx-auto shadow">
                {{ form_start(resetForm) }}
                    <div class="mb-3">
                        <label for="change_password_form_plainPassword_first">Nouveau mot de passe</label>
                        {{form_widget(resetForm.plainPassword.first, {'attr': {'class': 'form-control', 'autofocus':'autofocus'}})}}
                    </div>
                    <div class="mb-3">
                        <label for="change_password_form_plainPassword_second">Confirmation du nouveau mot de passe</label>
                        {{form_widget(resetForm.plainPassword.second, {'attr': {'class': 'form-control'}})}}
                    </div>
                    <div>
                        <input type="submit" class="btn btn-primary w-100 shadow" value="Réinitialiser">
                    </div>
                {{ form_end(resetForm) }}
            </div>
        </div>
    </div>
{% endblock %}

